# @skiesjs/express

The explicit Express 5 boundary for Skies slices. `endpoint` maps `Result<T>` to JSON and canonical status codes;
unexpected exceptions continue to the application's normal Express error middleware.
