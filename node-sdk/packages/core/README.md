# @skiesjs/core

Small, HTTP-agnostic primitives shared by Skies Node.js adapters: the `Result<T>` discriminated union, closed
`ErrorKind` catalog, stable `SkiesError` contract, and expected-error factories. It has no Express dependency.
