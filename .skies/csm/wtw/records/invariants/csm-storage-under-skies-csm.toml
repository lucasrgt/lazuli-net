schema = 1
id = "csm-storage-under-skies-csm"
kind = "invariant"
status = "active"
title = "Default Skies CSM storage to .skies/csm"
statement = "New Skies projects default their repository-local CSM storage root to .skies/csm while honoring an explicit csm.toml override."
violation = "The Skies scaffold defaults CSM to another root, or a Skies foundation command ignores the shared csm.toml storage override."
scopes = [
    "README.md",
    "src/Skies.Framework.Cli/CsmProject.cs",
    "templates/skies-app/csm.toml",
]
evidence = [
    """
Every `skies new` project includes the complete stack: `csm.toml`, the four
stores under `.skies/csm/`, all five protocols, portable skills, managed agent instructions, and
pre-commit and pre-push checks.""",
    """
`.skies/csm` is the Skies scaffold default, not a hard-coded tool path. The
shared `[storage].root` in `csm.toml` may override it for a repository""",
]
recorded_at = "2026-08-01T17:38:12.075615600Z"
recorded_by = "Lucas Tinoco"
recorded_commit = "773e17c728bff2bea7bd6327ab4bd895b3d2c92a"

[authority]
kind = "contract"
source = "README.md"
quote = "`.skies/csm` is the Skies scaffold default, not a hard-coded tool path."
