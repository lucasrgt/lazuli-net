schema = 1
id = "staged-check-preserves-directly-mapped-proofs"
kind = "invariant"
status = "active"
title = "Staged checks preserve directly mapped proofs"
statement = "Before commit, `skies check --staged` runs every directly mapped proof and defers only exhaustive fallbacks and browser or device execution."
violation = "A staged check omits a directly mapped proof or executes a deferred exhaustive, browser, or device fallback."
scopes = ["**/*"]
evidence = [
    "Before commit, `skies check --staged` runs every directly mapped proof and defers only exhaustive fallbacks and",
    "browser or device execution.",
]
recorded_at = "2026-08-12T04:15:20.647200800Z"
recorded_by = "Lucas Tinoco"
recorded_commit = "01591235e97ea970168b07345f5602d557b7f548"

[authority]
kind = "contract"
source = "docs/decisions/explicit-verification-boundaries.md"
quote = """
Before commit, `skies check --staged` runs every directly mapped proof and defers only exhaustive fallbacks and
browser or device execution."""

[[links]]
rel = "supersedes"
to = "wtw://invariant/staged-check-preserves-mapped-proofs"
basis = "The ADR states that this invariant preserves the same behavior under the explicit authority model."
