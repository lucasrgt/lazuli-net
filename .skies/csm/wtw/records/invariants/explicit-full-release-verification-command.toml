schema = 1
id = "explicit-full-release-verification-command"
kind = "invariant"
status = "active"
title = "Explicit full-release verification command"
statement = "Every repository exposes an explicit `--full` release verification command, whether invoked locally or by release automation."
violation = "A repository lacks an explicit release command that runs verification with `--full`."
scopes = ["**/*"]
evidence = [
    'Both modes expose `skies check --task "<release>" --full` through an explicit release command.',
    """
Every repository exposes an explicit `--full` release command, whether that command is invoked locally or by
release automation.""",
]
recorded_at = "2026-08-12T04:18:06.397538100Z"
recorded_by = "Lucas Tinoco"
recorded_commit = "01591235e97ea970168b07345f5602d557b7f548"

[authority]
kind = "adr"
source = "docs/decisions/explicit-verification-boundaries.md"
quote = """
Every repository exposes an explicit `--full` release command, whether that command is invoked locally or by
release automation."""
