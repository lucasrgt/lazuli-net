schema = 1
id = "pre-push-check-is-base-relative-fast"
kind = "invariant"
status = "superseded"
title = "Pre-push verification is base-relative and bounded"
statement = 'Before push, the checked hook runs `skies check --task "<review>" --base <target-revision> --fast` against the committed diff.'
violation = "The pre-push hook omits the base revision or `--fast`, validates an ambiguous scope, or does not repeat the bounded committed-diff review."
scopes = [
    "**/AGENTS.md",
    "**/CLAUDE.md",
    "**/lefthook.yml",
    "src/Skies.Framework.Cli/FoundationInstructions.cs",
    "templates/skies-app/**/*",
]
evidence = [
    """
Before push, the agent and hook run
`skies check --task "<review>" --base <target-revision> --fast`.""",
    "The pre-push hook repeats this bounded committed-diff review.",
]
recorded_at = "2026-07-30T20:00:46.709072300Z"
recorded_by = "Lucas Tinoco"
recorded_commit = "4f052f964f6c0b3504f6627fdc973840ef978688"

[authority]
kind = "contract"
source = "docs/decisions/explicit-verification-boundaries.md"
quote = "Before push, the checked hook runs a base-relative fast review of the committed diff."
