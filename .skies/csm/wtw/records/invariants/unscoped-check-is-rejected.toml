schema = 1
id = "unscoped-check-is-rejected"
kind = "invariant"
status = "active"
title = "Unscoped checks fail before gate execution"
statement = "A bare `skies check --task ...` invocation is invalid and exits before starting any gate."
violation = "A check without exactly one of `--staged`, `--affected`, `--base <revision>`, or `--full` starts gate execution."
scopes = [
    "src/Skies.Framework.Cli/FoundationWorkflowCommand.cs",
    "src/Skies.Framework.Cli/Program.cs",
    "tests/Skies.Framework.Cli.Tests/FoundationWorkflowCommandTests.cs",
]
evidence = [
    "Bare `skies check --task ...` is invalid and exits before any gate starts.",
    "choose an explicit scope: --staged, --affected, --base <revision>, or --full",
]
recorded_at = "2026-07-30T20:00:46.820758300Z"
recorded_by = "Lucas Tinoco"
recorded_commit = "4f052f964f6c0b3504f6627fdc973840ef978688"

[authority]
kind = "contract"
source = "docs/decisions/explicit-verification-boundaries.md"
quote = "An unscoped `skies check` exits before starting a gate."

[[links]]
rel = "supersedes"
to = "wtw://invariant/fail-closed-skies-check-before-completion"
basis = "The accepted ADR explicitly supersedes the former unscoped completion-check invariant with an explicit-scope contract."
