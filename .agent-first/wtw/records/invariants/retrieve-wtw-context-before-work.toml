schema = 1
id = "retrieve-wtw-context-before-work"
kind = "invariant"
status = "superseded"
title = "Retrieve WTW context before work"
statement = "At task start, agents run `dotnet tool run skies wtw explain` with the goal and expected path, treat returned active invariants as constraints and decisions as governing context, and rerun it when scope changes or context is compacted."
violation = "Work begins, continues after a scope change, or resumes after context compaction without retrieving the applicable WTW decisions and invariants."
scopes = [
    "**/.agent-first/wtw/**",
    "**/AGENTS.md",
    "**/CLAUDE.md",
    "**/GEMINI.md",
    "skies-plugin/skills/**",
]
evidence = [
    '+1. At task start, run `dotnet tool run skies wtw explain --task "<goal>" --path <expected-path>`.',
    "+3. Rerun `dotnet tool run skies wtw explain` when scope changes or context is compacted.",
]
recorded_at = "2026-07-27T04:00:37.986611Z"
recorded_by = "Lucas Tinoco"
recorded_commit = "e91425015330c1b55ed8cb24d0f1209ffacd10a6"

[authority]
kind = "policy"
source = "diff"
quote = """
1. At task start, run `dotnet tool run skies wtw explain --task "<goal>" --path <expected-path>`.
+2. Treat every returned active invariant as a constraint and every returned
+   decision as governing context.
+3. Rerun `dotnet tool run skies wtw explain` when scope changes or context is compacted."""
