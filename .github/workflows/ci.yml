name: CI

on:
  workflow_call:
  push:
    branches: [main]
  pull_request:

# Two native engines, one front door (per frontend/README): the .NET backend slice and the React/TS frontend
# slice each get a gate. A change to either fails CI on its own terms.
jobs:
  dotnet:
    name: backend (skies gate)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-dotnet@v6
        with:
          dotnet-version: "10.0.x"
      # The framework's own suite is small enough to stay exhaustive. This avoids a same-branch push comparing
      # against its freshly updated origin/HEAD and exercises every declaration × proof × TRX join on Linux.
      - run: dotnet run --project src/Skies.Framework.Cli/Skies.Framework.Cli.csproj -c Release -- gate --full Skies.Framework.slnx -c Release

  frontend:
    name: frontend-sdk (typecheck + lint + test)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend-sdk
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend-sdk/package-lock.json
      - run: npm ci
      - run: npm run check
