# Framework-development topology. The sample backend is the runnable product exercised by the root release gate;
# frontend-sdk owns and verifies the framework's TypeScript packages and sample fixtures in its dedicated CI job.

[workspace]
name = "skies"

[products.sample]
backend = "examples/sample-app/backend/Sample.Api"
